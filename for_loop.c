/* This program is a basic example of implementation of a for loop */
#include <stdio.h>

int main(){
    int n;
    printf("Enter the number of terms :");
    scanf("%d",&n);
    for (int i=0;i<n;i++)
    {printf("%d",n);
    }return 0;
}